# Enter your code here. Read input from STDIN. Print output to STDOUT
import numpy

n,m=map(int,input().split())
k=[]
for i in range(n):
    s=list(map(int,input().split()))
    k.append(s)
g=numpy.min(k, axis=1)
h=numpy.max(g)
print(h)
