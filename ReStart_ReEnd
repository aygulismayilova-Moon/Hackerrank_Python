# Enter your code here. Read input from STDIN. Print output to STDOUT
import re 
s=input()
d=input()
pat=re.compile(d)
match_=pat.search(s)
if not match_:
    print("(-1, -1)")
while match_:
    print('({0}, {1})'.format(match_.start(),match_.end()-1))
    match_=pat.search(s,match_.start()+1)
